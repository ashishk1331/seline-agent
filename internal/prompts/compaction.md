Summarize this conversation for an AI agent that needs to continue its work.

Preserve exactly:
- Tasks requested and their current status (done / in-progress / blocked)
- Tool calls made and their outcomes (commands run, files read/written, URLs fetched, search results)
- Any errors encountered and how they were handled
- Key facts, values, filenames, paths, and decisions that future steps may depend on
- What the agent was doing last, so it can resume without repeating work

Be terse. No filler. Output only the summary.